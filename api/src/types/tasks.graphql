type Query {
    tasks: [Task]
}

type Task {
    id: ID!
    title: String!
    text: String!
}

type Mutation {
    deleteTask(id: ID!): [Task]
    createTask(title: String, type: String, status: String): ID!
}